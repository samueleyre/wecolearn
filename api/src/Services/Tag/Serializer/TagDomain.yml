App\Services\Tag\Entity\TagDomain:
    exclusion_policy: all
    access_order:     alphabetical
    properties:
        id:
          groups: [ input, output, putTag, profile, admin-tags ]
        name:
          groups: [ input, output, search, profile, public-profile, admin-users, admin-tags ]
        emoji:
          groups: [ input, output, search, profile, public-profile, admin-users, admin-tags ]
        created:
          groups: [ admin-tags ]




