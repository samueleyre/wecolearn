App\Services\Domain\Entity\Domain:
    exclusion_policy: all
    access_order:     alphabetical
    properties:
        id:
          groups: [ input, output, admin-users, admin-user-patch, admin-domains, profile ]
        name:
          groups: [ input, output, admin-users, admin-domains, profile ]
        count:
          groups: [ admin-domains ]
        image:
          groups: [ input, output, admin-domains, profile ]
        isMain:
          groups: [ output, profile ]
        created:
          groups: [ admin-domains ]


