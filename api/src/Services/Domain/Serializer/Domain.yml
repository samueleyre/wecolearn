App\Services\Domain\Entity\Domain:
    exclusion_policy: all
    access_order:     alphabetical
    properties:
        id:
          groups: [
              admin-community,
              admin-users,
              admin-user-patch,
              admin-domains,
              input,
              output,
              profile
          ]
        name:
          groups: [
              admin-community,
              admin-users,
              admin-domains,
              input,
              output,
              profile
          ]
        count:
          groups: [
              admin-community,
              admin-domains
          ]
        image:
          groups: [
              admin-community,
              admin-domains,
              input,
              output,
              profile
          ]
        isMain:
          groups: [
              output, profile
          ]
        inviteToken:
          groups: [
              admin-community,
              admin-domains
          ]
        created:
          groups: [ admin-domains ]


