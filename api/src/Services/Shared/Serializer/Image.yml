App\Services\Shared\Entity\Image:
    exclusion_policy: all
    access_order:     alphabetical
    properties:
        id:
          groups: [ input, output ]
        version:
          groups: [ output, search, profile, public-profile, message, admin-community]
        publicId:
          groups: [ output, search, profile, public-profile, message, admin-community ]
        created:
          groups: [ output]
        filename:
          groups: [ input ]


