App\Services\User\Entity\User:
    exclusion_policy: all
    xml_root_name:    page
    access_order:     alphabetical
    properties:
      id:
        groups: [ input, search, message, profile, public-profile,  admin-users, thread , notif]
      created:
        groups: [ input, profile, admin-users]
      firstName:
        groups: [ input, output, create, search, message, profile, public-profile, admin-users, notif ]
      lastName:
        groups: [ input, output, create, search, message, profile, public-profile, admin-users ]
      profilUrl:
        groups: [ input, output, search, message, profile, public-profile, admin-users ]
      biographie:
        groups: [ input, output, create, search, profile, public-profile ]
      intensity:
        groups: [ input, profile, public-profile ]
      atmosphere:
        groups: [ input, profile, public-profile ]
      latitude:
        groups: [ input, create, admin-users, profile ]
      longitude:
        groups: [ input, create, admin-users, profile ]
      tags:
        groups: [ input, create, search, profile, public-profile, admin-users ]
      sentMessages:
        groups: [ input ]
        max_depth: 4
      receivedMessages:
        groups: [ input ]
        max_depth: 4
      image:
        type: App\Services\User\Entity\Image
        groups: [ input, output, search, message, profile, public-profile, notif ]
      showProfil:
        groups: [ input, profile, admin-users ]
      newMessageNotification:
        groups: [ input, profile ]
      newMatchNotification:
        groups: [ input, profile ]
      newsletter:
        groups: [ input, create, profile ]
      slackAccounts:
        groups: [ input, search ]
      userNotified:
        groups: [ input ]
      notificationSubscribe:
        groups: [ input ]
      domains:
        groups: [ admin-users ]
      deleted:
        groups: [ admin-users ]


