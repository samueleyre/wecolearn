App\Services\User\Entity\Image:
    exclusion_policy: all
    access_order:     alphabetical
    properties:
        id:
          groups: [ input, output, notif ]
        version:
          groups: [ output, search, profile, public-profile, message, notif]
        publicId:
          groups: [ output, search, profile, public-profile, message , notif]
        created:
          groups: [ output]
        filename:
          groups: [ input, notif ]


