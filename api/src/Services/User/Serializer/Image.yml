App\Services\User\Entity\Image:
    exclusion_policy: all
    access_order:     alphabetical
    properties:
        id:
          groups: [ input, output ]
        version:
          groups: [ output, search, profile, message ]
        publicId:
          groups: [ output, search, profile, message ]
        created:
          groups: [ output ]
        filename:
          groups: [ input ]


