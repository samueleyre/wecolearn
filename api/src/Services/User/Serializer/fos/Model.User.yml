FOS\UserBundle\Model\User:
    exclusion_policy: none
    access_order:     alphabetical
    properties:
        username:
          groups: [ input, output ]
        email:
          groups: [ input, output, admin-users ]
        password:
          groups: [ input ]
        roles:
          groups: [ input, output, profile, admin-users ]

